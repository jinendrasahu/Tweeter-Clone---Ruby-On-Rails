 <div class="container">

 <div class="col">
<%= render 'trends' %>
</div>
<div class="col">
 <% if user_signed_in? %>
<%= render 'profile' %>
<% end %>
<%= render 'feed' %>
</div>
 <div class="col">
<%= render 'who-to-follow' %>
</div>
</div> 